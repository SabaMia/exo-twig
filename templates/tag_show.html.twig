{% extends 'base.html.twig' %}

{% block metaTitle %}
    Tag: {{ tag.title }}
{% endblock %}

{% block main %}
        <main>
            <div class="center lHeight200">
                <h1 style="background-color:{{ tag.color }};">{{ tag.title }}</h1>
            </div>
        </main>
{% endblock %}



