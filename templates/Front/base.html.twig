{# Création du dossier BASE qui va servir de layout à toutes les pages #}
<!doctype html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport"
              content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Patrick+Hand&display=swap" rel="stylesheet">
        <link rel="stylesheet" href={{ asset('style.css') }}>
        {# On créé des blocks à chaque fois qu'on veut insérer du contenu personnalisé selon l'article
        puis on met un endblock pour fermer le block #}
        <title>{% block metaTitle %}{% endblock metaTitle %}</title>
    </head>
    <body>
        <header>
            <nav>
                <div>
                    <img src={{ asset('media/logo.png') }} alt="logo" class="resp">
                </div>
                {# insertion d'un formalaire avec une méthode get afin de récupérer le mot tapé dans la recherche #}
                <form action="{{ path('search') }}" method="get">
                    <input type="text" name="q" placeholder="recherche...">
                    <input type="submit" value="Rechercher">
                </form>
                {# Ensuite il faut aller modifier la route search dans ArticleController #}
                <div>
                    <a href={{ path('article_list',)}}><img src={{ asset('media/menu.png') }} alt="menu" class="resp"></a>
                </div>
            </nav>
        </header>
        {% block main %}
        {# le contenu des articles contenu dans leurs balises main s'affichera ici #}
        {% endblock main %}
        <footer>
        </footer>
    </body>
</html>